Meta:

Narrative:
As a user
I want to see timeline in descending order of timestamp
So that I can see latest updates at top.

Scenario: scenario description
Given timeline has some data to show
When I load timeline
Then timeline should show first 10 latest updates by timestamp.